# context
I need a portable way to distribute criu, zero-install distribution in a single tarball, the best strategy is a "Hermetic Bundle" rather than a single static binary.

Since you can't install packages on the target, use a Docker container to gather everything once:

Build CRIU and the Plugin normally (dynamically) inside a container.

Gather Dependencies: Use a tool like linuxdeploy or a simple script using ldd to copy every .so file that criu and cuda_plugin.so need into the lib/ folder.

The Wrapper Script: Create a script (criu-wrapper) that forces CRIU to use the local libraries:

# Steps

1. Plan first
2. Ask if ok
3. Iterate
4. Implement

# References

- `criu/Makefile`: is used to build criu the way we want we modified it to be statically
- `release/Dockerfile`: has the way we build the binary to distribute
- `.github/workflows/releaase.yaml`: is where we releaase the package in github
